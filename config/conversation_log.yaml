enabled:               true
interfaces:            []
agent_modes:           [all]
turn_context_priority: 20     # after memory (10)
turn_context_persist: false   # if true, injections persist in history for all turns

recency_hint_minutes:  120    # inject recent session hint if within this window
debug:                 false